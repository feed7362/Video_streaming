apiVersion: v1
kind: Service
metadata:
  name: {{ include "video-streaming.fullname" . }}-backend
  labels:
    {{- include "video-streaming.labels" (dict "ctx" . "component" "backend") | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "video-streaming.selectorLabels" (dict "ctx" . "component" "backend") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
